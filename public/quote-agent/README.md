# Deprecated

`index.html` is deprecated and should not receive new productConfigAgent UI work.
Keep backend/API changes in `src/features/productConfigAgent` and use the current
frontend surface instead.
