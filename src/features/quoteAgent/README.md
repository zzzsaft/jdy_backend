# quoteAgent Compatibility Layer

The product configuration implementation has moved to
`src/features/productConfigAgent`.

Do not add new product configuration code here. This directory only preserves
legacy imports and `/quoteAgent` route compatibility during migration.
