/**
 * Feature route definitions aggregated from src/features/<feature>/routes.ts
 *
 * Keep this list as the single place to register new feature routes.
 */
export const FeatureRoutes: Array<unknown> = [];
